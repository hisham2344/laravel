
 version: 0.2
 phases:
   install:
     commands:
       - composer install
   build:
     commands:
       - cp .env.example .env
       - php artisan key:generate
 artifacts:
   files:
     - '**/*'
